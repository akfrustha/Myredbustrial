
import 'package:flutter/material.dart';

class StudentThree extends StatelessWidget {
  const StudentThree({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Student Page Three',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}